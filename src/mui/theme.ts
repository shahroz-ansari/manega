import type { ThemeOptions } from '@mui/material'

export const themeLight: ThemeOptions = {
  palette: {
    mode: 'light',
    primary: {
      main: '#cc00dd',
    },
    secondary: {
      main: '#19857b',
    },
  },
}
